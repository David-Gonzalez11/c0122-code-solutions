/* exported head */
/*
-Define the function name
-create a new variable for the determined output later
- get the first element at the first index
-return the varibale created in the beginning of these steps
*/
function head(array) {
  var first = array[0];
  return first;
}
