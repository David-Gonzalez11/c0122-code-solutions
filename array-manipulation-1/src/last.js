/* exported last */
/*
define the function and it name
-define a new varibale to assign it to the output
-get the last element of array and subtract 1 from its length
-return the varibale that was created in the beginnning
*/

function last(array) {
  var last = array[array.length - 1];
  return last;
}
