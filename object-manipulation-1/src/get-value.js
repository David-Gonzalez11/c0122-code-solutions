/* exported getValue */
/*
define the function and its name
-assign a new variable
- get the key value within the object
return the value of the key from the object object
*/
function getValue(object, key) {
  var value = object[key];
  return value;
}
