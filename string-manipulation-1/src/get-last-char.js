/* exported getLastChar */
/*
define the function name
- create a new varubale last
- get the last chracter of the string by subtracting 1 from the string length

*/

function getLastChar(string) {
  var last = string.charAt(string.length - 1);
  return last;
}
