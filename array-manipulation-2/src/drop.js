/* exported drop */
/*
create a new function and give it the parameters of array and count
use the slice method and give it the argunent count
return

*/
function drop(array, count) {
  return array.slice(count);
}
