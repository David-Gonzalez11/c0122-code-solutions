// /* exported take */
/*
create a new function and use the slice method in order to start the array at 0
give it the argument count so that it goes up to a certain count and ends there
*/

function take(array, count) {
  return array.slice(0, count);
}
